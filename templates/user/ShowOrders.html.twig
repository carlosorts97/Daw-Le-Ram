{% extends 'base.html.twig' %}

{% block error %}{% endblock %}

{% block body %}
    <div class="container margin_scroll signika">
        <h3 class="">Mis Pedidos</h3>
    </div>
    {% for order in orders %}
        <div class="container mt-5 mb-5 border-bottom text-center">
            <div class="row">
                <div class="col-lg-4 col-md-4 col-4">
                    <p>Articulo:<h3 class="text-secondary">{{ order.article.name }}</h3></p>
                </div>
                <div class="ccol-lg-4 col-md-4 col-4">
                    <p>Talla:<h3 class="text-secondary">{{ order.size }}</h3></p>
                </div>
                <div class="col-lg-4 col-md-4 col-4">
                    <p>Precio total:<h3 class="text-secondary">{{ order.totalPaid }}€</h3></p>
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}